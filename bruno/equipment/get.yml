info:
  name: get
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/equipment"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
