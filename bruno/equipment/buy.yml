info:
  name: buy
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/equipment"
  body:
    type: json
    data: |-
      {
        "type":"pickaxe"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
