info:
  name: hire
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/miners"
  body:
    type: json
    data: |-
      {
        "type": "small_miner"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
