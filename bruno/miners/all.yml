info:
  name: all
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/miners/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
