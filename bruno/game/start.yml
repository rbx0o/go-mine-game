info:
  name: start
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/game/start"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
