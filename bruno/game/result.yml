info:
  name: result
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/game/result"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
