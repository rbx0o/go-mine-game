info:
  name: stop
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/game/stop"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
